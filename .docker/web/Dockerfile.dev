FROM node:12.15.0-alpine

RUN apk add --update --no-cache \
    python \
    make \
    g++
 
WORKDIR /usr/app
COPY ./.env ./
COPY ./package*.json ./
RUN npm install -g

EXPOSE 3000

CMD ["npm", "run", "dev"]
